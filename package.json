{
    "name": "@react-kit/root",
    "version": "1.0.0",
    "private": true,
    "source": "public/index.html",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "serve": "parcel serve",
        "build": "lerna run build",
        "watch": "lerna run --parallel watch",
        "clean": "lerna clean"
    },
    "devDependencies": {
        "lerna": "^4.0.0",
        "parcel": "^2.3.2"
    }
}
